av>=18.1.0
opencv-python-headless>=5.0.0
numpy>=2.5.0
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
pillow>=12.0.0

# Optional: Intel iGPU/NPU acceleration. The pipeline falls back to
# onnxruntime CPU when this is absent.
openvino>=2026.3

# Speech. ctranslate2 rather than torch, which is the only reason audio is
# affordable here: the runtime stays ONNX Runtime, numpy and this.
faster-whisper>=1.2

# Desktop window and browser uploads
pywebview>=6.2
python-multipart>=0.0.20
