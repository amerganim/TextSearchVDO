av>=18.1.0
opencv-python-headless>=5.0.0
numpy>=2.5.0
fastapi>=0.141.0
uvicorn[standard]>=0.52.0
pillow>=12.0.0

# Every model except speech runs on this. Declared explicitly even though
# faster-whisper happens to require it too: detection, CLIP and captioning all
# stop working without it, and depending on the speech library's dependency
# list to supply the vision pipeline means "I do not need audio" quietly
# breaks everything else.
onnxruntime>=1.14,<2

# Optional: Intel iGPU/NPU acceleration. The pipeline falls back to
# onnxruntime CPU when this is absent.
openvino>=2026.3

# Speech. ctranslate2 rather than torch, which is the only reason audio is
# affordable here: the runtime stays ONNX Runtime, numpy and this.
faster-whisper>=1.2

# QR code for phone pairing. Pure Python, no dependencies of its own.
segno>=1.6

# Desktop window and browser uploads
pywebview>=6.2
python-multipart>=0.0.20
